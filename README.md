# client

Code client web

## Getting started

### Development

        cd client
        npm install
        npm start

### Docker

Build docker

        docker build --tag <tag_name> .

Run docker

        docker run -d -p 4200:4200 <tag_name>
